name: Update SportTV1 Link

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar Dependências
        run: |
          pip install selenium-wire webdriver-manager blinker==1.7.0

      - name: Executar Script
        run: python extrair_link.py

      - name: Git Push
        run: |
          git config --global user.name "Sports Bot"
          git config --global user.email "bot@github.com"
          if [ -f sporttv1.m3u ]; then
            git add sporttv1.m3u
            git commit -m "Auto Update $(date +'%H:%M')" || echo "Sem mudanças"
            git push
          else
            echo "Ficheiro não gerado."
            exit 1
          fi
